export enum TransactionType {
  TRANSFER = "TRANSFER",
  WITHDRAW = "WITHDRAW",
  DEPOSIT = "DEPOSIT",
}

export enum TransactionStatus {
  PENDING = "PENDING",
  COMPLETED = "COMPLETED",
  FAILED = "FAILED",
}